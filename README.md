# Compilador
Es un programa en Java que genera automáticamente los componentes de un compilador utilizando JFlex y Java CUP, creando el analizador léxico y sintáctico a partir de archivos .flex. Además, mueve y organiza los archivos generados (sym.java y Sintax.java) dentro del proyecto para su uso en el compilador.
